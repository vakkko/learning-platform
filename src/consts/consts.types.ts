export interface NavItem {
  linkTo: string;
  src?: string;
  to?: string;
}

export interface carouselItemsTypes {
  imgSrc: string;
  heading: string;
  text: string;
  buttonText: string;
  link?: string;
}
